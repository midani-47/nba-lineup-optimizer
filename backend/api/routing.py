from django.urls import re_path
from . import consumers

# Define WebSocket URL patterns
# This is a placeholder - we'll implement actual consumers later
websocket_urlpatterns = [
    # re_path(r'ws/lineups/$', consumers.LineupConsumer.as_asgi()),
] 